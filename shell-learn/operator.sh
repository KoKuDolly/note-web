#!/bin/sh

# 算术 + - \* / % = == !=

# 关系 -eq -ne -gt -lt -ge -le

# 布尔 ! -o -a

# 逻辑 && ||

# 字符串 = != -z -n $

# 文件测试 -b -c -d -f -g -k -p -u -r -w -x -s -e

# result=$[a + b]
# 等同于：
# result=`expr $a + $b `
# 前一种的运算速度更快